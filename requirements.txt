dash>=4.0.0
dash-ag-grid>=31.0.0
dash-bootstrap-components>=1.7.1
dash-daq>=0.6.0
pandas>=2.2.3
numpy>=2.0.0  # 1.x is EOL (1.26 ended Sep 2025); suite is tested on numpy 2.x
plotly>=6.0.0
ruamel.yaml>=0.18.10
PyYAML>=6.0.1  # Used by watchlist_loader, pathogen_loader, and other modules
biopython>=1.85
tqdm>=4.67.1
flask>=3.1.0
requests>=2.32.3
diskcache>=5.6.0
psutil>=6.0.0  # Required for Dash DiskcacheManager
multiprocess>=0.70.0  # Required for Dash background callbacks
dill>=0.3.0  # Required for Dash background callbacks
openpyxl>=3.1.0  # Required for XLSX export

# Note: Nextflow must be installed separately
# Installation: curl -s https://get.nextflow.io | bash
# Or via conda: conda install -c bioconda nextflow

# Security note: Reviewed 2026-06. One known CVE: diskcache 5.6.3
# (CVE-2025-69872, pickle deserialization -> code execution). Low exposure
# here: the DiskcacheManager cache is per-process under user-owned
# ~/.nanometa (app.py:_ensure_background_callback_manager) and requires
# local write access to exploit. No patched release exists; 5.6.3 is the
# latest. Re-evaluate if Dash gains a non-pickle background-callback backend.
# numpy floored at 2.x: the 1.x series is end-of-life (1.26 ended Sep 2025)
# and the test suite runs against numpy 2.x (verified 2026-06 on 2.4.4).
