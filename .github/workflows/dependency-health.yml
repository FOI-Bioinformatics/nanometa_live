name: Dependency health

# Quarterly dependency check (plus manual trigger). Surfaces end-of-life core
# dependencies, outdated packages, and known CVEs so they are noticed on a
# cadence rather than the next time something breaks. The EOL check fails the
# run (red build -> notification) when a monitored series is end-of-life; the
# outdated/audit steps are informational.

on:
  schedule:
    # 06:00 UTC on the 1st of Jan, Apr, Jul, Oct (quarterly).
    - cron: '0 6 1 1,4,7,10 *'
  workflow_dispatch: {}

concurrency:
  group: dependency-health
  cancel-in-progress: true

jobs:
  dependency-health:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip

      - name: Install package
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: End-of-life check (fails on EOL series)
        run: python scripts/check_dependency_eol.py

      - name: Outdated packages (informational)
        if: always()
        run: pip list --outdated || true

      - name: Security audit (fails on new vulnerabilities)
        if: always()
        run: |
          pip install pip-audit
          # Accept the one known, unfixable finding so the gate still
          # catches NEW CVEs instead of being a no-op:
          #   CVE-2025-69872 - diskcache 5.6.3 pickle deserialization.
          # Low exposure: the cache is per-process under user-owned
          # ~/.nanometa and needs local write access to exploit; no patched
          # release exists. Drop this ignore if a fix ships or Dash gains a
          # non-pickle background-callback backend.
          pip-audit --ignore-vuln CVE-2025-69872
